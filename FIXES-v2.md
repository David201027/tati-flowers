# TATI Flowers — fixes v2

Исправлено:
- каталог больше не маскирует ошибку Google Sheets старыми демо-карточками;
- при загрузке сайт принудительно запрашивает свежий каталог (`refresh=1`);
- если Google Sheets недоступен, показывается понятное состояние каталога вместо Blush/Wild Garden;
- окно «Доставка и оплата» полностью скрыто, пока пользователь его не откроет;
- кнопка «Заказать» в шапке использует основной стиль кнопок;
- запрос к Apps Script на сервере следует redirect и отключает кэш;
- пустая/невалидная ссылка на фото больше не ломает карточку.
